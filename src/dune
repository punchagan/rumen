(executable
 (name sw)
 (modes js)
 (libraries brr))

(executable
 (name app)
 (modes js)
 (libraries brr))

(rule
 (with-stdout-to
  workflow.ml
  (progn
   (echo "let workflow_yml = {yml|")
   (cat workflow.yml)
   (echo "|yml}\n"))))
